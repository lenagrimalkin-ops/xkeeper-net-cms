{
  "collectionName": "components_shared_ctas",
  "info": { "displayName": "CTA Button", "icon": "cursor" },
  "options": {},
  "attributes": {
    "label": { "type": "string", "required": true },
    "url": { "type": "string", "required": true },
    "style": {
      "type": "enumeration",
      "enum": ["primary", "ghost", "quiet"],
      "default": "primary"
    }
  }
}
